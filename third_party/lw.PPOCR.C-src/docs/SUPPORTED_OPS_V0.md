# PP-OCRv6 Tiny ONNX Operator Analysis (V0)

> Generated by `converter/analyze_onnx.py`. Do not edit operator counts by hand.

## Scope and decision boundary

This report describes only the exact bundled PP-OCRv6 tiny DET, CLS, and REC models.
It is not a promise to support arbitrary ONNX graphs. The v0.1 runtime supports the exact REC and fixed-batch CLS graphs,
FP32, CPU, scalar, and single-threaded. ONNX is a converter dependency only.

## Model inventory

| Model | SHA-256 | Opset | Nodes | Initializers | Weight bytes | Input | Output | Representative FLOPs |
|---|---|---:|---:|---:|---:|---|---|---:|
| DET | `193bab7a04fca699a6c82e6abb5b81bdb28177f0abd4062552b04908dafb19f8` | ai.onnx:14 | 242 | 169 | 1,713,680 | `x` [DynamicDimension.0, 3, DynamicDimension.1, DynamicDimension.2] | `fetch_name_0` [ConvTranspose_459_o0__d0, 1, ConvTranspose_459_o0__d2, ConvTranspose_459_o0__d3] | 2.797 GFLOPs |
| CLS | `dd8b2b61983d76ab230a58da9e0e0e84956b71c3877f2ce6e438fe22d74d2cf2` | ai.onnx:7 | 143 | 148 | 982,004 | `x` [DynamicDimension.0, 3, 80, 160] | `fetch_name_0` [DynamicDimension.0, 2] | 46.428 MFLOPs |
| REC | `9ef676d6ed3c88256a2d92c640c44f25b0c40947e111b14b8be8f594091563e6` | ai.onnx:11 | 219 | 120 | 4,418,096 | `x` [DynamicDimension.0, 3, 48, DynamicDimension.1] | `fetch_name_0` [DynamicDimension.0, floor(floor(floor(DynamicDimension.1/2 - 1/2)/2)/2 - 1/2) + 1, 6906] | 368.964 MFLOPs |

Representative shapes are DET `[1,3,640,640]`, CLS `[1,3,80,160]`, and
REC `[1,3,48,320]`. FLOPs count multiply-add as two operations and exclude
pre/post-processing and elementwise costs, so they are estimates rather than benchmark data.

## Operator union (25 types)

| Operator | DET | CLS | REC | Required by |
|---|---:|---:|---:|---|
| Add | 36 | 5 | 52 | CLS, DET, REC |
| AveragePool | 0 | 0 | 1 | REC |
| BatchNormalization | 0 | 27 | 4 | CLS, REC |
| Concat | 2 | 1 | 0 | CLS, DET |
| Conv | 83 | 32 | 37 | CLS, DET, REC |
| ConvTranspose | 2 | 0 | 0 | DET |
| Div | 13 | 0 | 10 | DET, REC |
| Erf | 13 | 0 | 10 | DET, REC |
| GlobalAveragePool | 8 | 3 | 0 | CLS, DET |
| HardSigmoid | 13 | 30 | 5 | CLS, DET, REC |
| Identity | 0 | 7 | 58 | CLS, REC |
| MatMul | 0 | 1 | 2 | CLS, REC |
| MaxPool | 1 | 0 | 0 | DET |
| Mul | 39 | 31 | 25 | CLS, DET, REC |
| ReduceMean | 5 | 0 | 3 | DET, REC |
| Relu | 20 | 2 | 3 | CLS, DET, REC |
| Reshape | 0 | 1 | 0 | CLS |
| Resize | 6 | 0 | 0 | DET |
| Shape | 0 | 1 | 0 | CLS |
| Sigmoid | 1 | 0 | 0 | DET |
| Slice | 0 | 1 | 0 | CLS |
| Softmax | 0 | 1 | 1 | CLS, REC |
| Squeeze | 0 | 0 | 3 | REC |
| Transpose | 0 | 0 | 3 | REC |
| Unsqueeze | 0 | 0 | 2 | REC |

## Per-model findings

### DET

Operators: `Add` × 36, `Concat` × 2, `Conv` × 83, `ConvTranspose` × 2, `Div` × 13, `Erf` × 13, `GlobalAveragePool` × 8, `HardSigmoid` × 13, `MaxPool` × 1, `Mul` × 39, `ReduceMean` × 5, `Relu` × 20, `Resize` × 6, `Sigmoid` × 1.

- Dynamic tensors after ONNX shape inference: 243.
- Shape/metadata nodes: 0.
- Constant-fold candidates: 0.
- Identity nodes removable by the converter: 0.
- Conv + BatchNormalization fusion candidates: 0.
- Estimated heavy-operator work: 2.797 GFLOPs.
- FLOPs by heavy operator: Conv 2.584 GFLOPs, ConvTranspose 212.992 MFLOPs.

### CLS

Operators: `Add` × 5, `BatchNormalization` × 27, `Concat` × 1, `Conv` × 32, `GlobalAveragePool` × 3, `HardSigmoid` × 30, `Identity` × 7, `MatMul` × 1, `Mul` × 31, `Relu` × 2, `Reshape` × 1, `Shape` × 1, `Slice` × 1, `Softmax` × 1.

- Dynamic tensors after ONNX shape inference: 141.
- Shape/metadata nodes: 3.
- Constant-fold candidates: 0.
- Identity nodes removable by the converter: 7.
- Conv + BatchNormalization fusion candidates: 27.
- Estimated heavy-operator work: 46.428 MFLOPs.
- FLOPs by heavy operator: Conv 46.423 MFLOPs, MatMul 5.120 KFLOPs.

### REC

Operators: `Add` × 52, `AveragePool` × 1, `BatchNormalization` × 4, `Conv` × 37, `Div` × 10, `Erf` × 10, `HardSigmoid` × 5, `Identity` × 58, `MatMul` × 2, `Mul` × 25, `ReduceMean` × 3, `Relu` × 3, `Softmax` × 1, `Squeeze` × 3, `Transpose` × 3, `Unsqueeze` × 2.

- Dynamic tensors after ONNX shape inference: 220.
- Shape/metadata nodes: 0.
- Constant-fold candidates: 0.
- Identity nodes removable by the converter: 58.
- Conv + BatchNormalization fusion candidates: 2.
- Estimated heavy-operator work: 368.964 MFLOPs.
- FLOPs by heavy operator: Conv 323.741 MFLOPs, MatMul 45.222 MFLOPs.

## REC-first runtime surface

The exact REC graph contains `Add`, `AveragePool`, `BatchNormalization`, `Conv`, `Div`, `Erf`, `HardSigmoid`, `Identity`, `MatMul`, `Mul`, `ReduceMean`, `Relu`, `Softmax`, `Squeeze`, `Transpose`, `Unsqueeze`.

The REC converter removes verified Identity aliases. The remaining executable
operator requirement derived from this exact model is:

`Add`, `AveragePool`, `BatchNormalization`, `Conv`, `Div`, `Erf`, `HardSigmoid`, `MatMul`, `Mul`, `ReduceMean`, `Relu`, `Softmax`, `Squeeze`, `Transpose`, `Unsqueeze`.

This analysis-derived set is not a frozen LWM contract. Runtime implementation
status and independent reference-test coverage are tracked in `scalar-kernels.md`.

## REC dynamic-width propagation

REC input: `x` [DynamicDimension.0, 3, 48, DynamicDimension.1].

REC output: `fetch_name_0` [DynamicDimension.0, floor(floor(floor(DynamicDimension.1/2 - 1/2)/2)/2 - 1/2) + 1, 6906].

The batch, channel, and height constraints remain fixed for the MVP while input width
is symbolic. ONNX shape inference propagates width through convolution/pooling and into
the sequence dimension. Dynamic output-producing nodes by operator are: `Add` × 52, `AveragePool` × 1, `BatchNormalization` × 4, `Conv` × 37, `Div` × 10, `Erf` × 10, `HardSigmoid` × 5, `Identity` × 58, `MatMul` × 2, `Mul` × 25, `ReduceMean` × 3, `Relu` × 3, `Softmax` × 1, `Squeeze` × 3, `Transpose` × 3, `Unsqueeze` × 2. The converter must preserve only this PP-OCR-specific width expression; it must
not introduce a general symbolic-shape engine.

## Shape and converter-removable work

- `Identity` is structurally removable after verifying graph outputs and aliases.
- `Shape`-derived integer subgraphs are metadata candidates and should be evaluated offline
  whenever all required dimensions are fixed or expressible by the supported REC width rule.
- Initializer-only subgraphs are constant-fold candidates.
- Conv + BatchNormalization is a low-risk fusion candidate but requires FP32 equivalence tests.
- Data-path `Transpose`, `Squeeze`, `Unsqueeze`, `Reshape`, and arithmetic operations remain
  runtime work unless analysis proves that a specific node is metadata-only or foldable.

## Main compute conclusion

The representative FLOP estimate is dominated by convolution and matrix multiplication.
The first performance work should therefore follow correctness-tested scalar Conv and MatMul;
metadata operators should be minimized in the converter. No kernel is justified solely because
it exists in ONNX—the exact model/operator matrix above is the requirement source.

## Runtime handoff requirements

1. Lock conversion to the exact verified REC model identity and opset.
2. Remove only structurally verified aliases and retain every required data-path node.
3. Require an independent NumPy or ONNX reference test for every LWM operator.
4. Compare complete-graph output before exposing a public inference call; enforce this with the private executor reference test.
